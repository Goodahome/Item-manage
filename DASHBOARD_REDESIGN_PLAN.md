# 首页重设计方案 - Discord风格布局

## 📐 布局结构

```
┌─────────────────────────────────────────────────────┐
│  TopAppBar（保持不变）                                  │
├──┬───────────────────────────────────────────────────┤
│  │  SearchBox（保持不变，固定在顶部）                     │
├──┼──┬────────────────────────────────────────────────┤
│左 │右 │  子容器横向滚动区                                  │
│侧 │侧 │  [○ 子容器1] [○ 子容器2] [○ 子容器3] [+]          │
│  │上 ├────────────────────────────────────────────────┤
│容 │  │                                                │
│器 │  │  物品列表（垂直滚动）                              │
│图 │右 │  ┌──────────────────────────────────┐          │
│标 │侧 │  │ [○] 物品A                         │          │
│列 │下 │  │     描述：xxx | 数量：5           │          │
│  │  │  ├──────────────────────────────────┤          │
│[○]│  │  │ [○] 物品B                         │          │
│[○]│  │  │     描述：xxx | 数量：3           │          │
│[○]│  │  └──────────────────────────────────┘          │
│[+]│  │                                                │
└──┴──┴────────────────────────────────────────────────┘
```

## 🎯 功能需求

### 1. 左侧容器图标列
- ✅ 固定宽度：70dp
- ✅ 垂直排列，可滚动
- ✅ 圆形图标（56dp直径）
- ✅ 显示容器图标或首字母
- ✅ 小红点通知（未读物品数量）
- ✅ 选中状态：蓝色边框或背景高亮
- ✅ 底部"+"按钮添加新容器
- ✅ 点击切换选中容器

### 2. 右侧顶部子容器区
- ✅ 横向滚动
- ✅ 圆形图标（48dp直径）
- ✅ 图标下方显示子容器名称
- ✅ 可能有徽章（物品数量）
- ✅ 末尾"+"按钮添加子容器
- ✅ 点击子容器图标导航或展开

### 3. 右侧底部物品列表
- ✅ 垂直滚动
- ✅ 每行显示：图标 + 名称 + 描述 + 数量
- ✅ 圆形物品图标（40dp直径）
- ✅ 主标题：物品名称（粗体）
- ✅ 副标题：物品描述或状态
- ✅ 右侧：数量等附加信息
- ✅ 点击打开详情/编辑
- ✅ 空状态提示："此容器为空"

## 🔄 交互流程

1. **初始加载**：
   - 左侧显示所有顶层容器图标
   - 默认选中第一个容器（或之前选中的容器）
   - 右上显示选中容器的子容器
   - 右下显示选中容器的物品

2. **点击左侧容器**：
   - 高亮选中的容器
   - 加载并显示该容器的子容器（右上）
   - 加载并显示该容器的物品（右下）
   - 保存选中状态到 SharedPreferences

3. **点击右上子容器**：
   - 可选：进一步展开显示子容器的物品
   - 或：导航到子容器详情页

4. **点击右下物品**：
   - 打开物品详情/编辑页面

5. **搜索功能**：
   - 顶部搜索栏输入关键词
   - 同时过滤左侧容器和右侧物品
   - 显示搜索结果

## 🎨 视觉设计

### 颜色方案
- 左侧容器列背景：`ColorHelpers.getGroup1NavBarColor()`
- 右侧内容区背景：`ColorHelpers.getGroup2PageBgColor()`
- 容器图标背景：`ColorHelpers.getGroup3CardBgColor()`
- 选中状态边框：`ColorHelpers.getGroup5FabColor()`
- 小红点通知：`ColorHelpers.getGroup5AlertCardColor()`

### 动画效果
- 容器切换：淡入淡出（300ms）
- 内容加载：滑动动画（200ms）
- 选中状态：缩放动画（150ms）

## 📝 实施步骤

### Phase 1: 创建基础组件（已完成✅）
- [x] 创建设计文档

### Phase 2: 左侧容器列表
- [ ] `WarehouseIconItem` - 单个容器图标组件
- [ ] `WarehouseSidebarColumn` - 左侧容器列组件
- [ ] 添加选中状态管理
- [ ] 添加小红点通知徽章
- [ ] 添加底部"+"按钮

### Phase 3: 右侧子容器区
- [ ] `SubWarehouseIcon` - 子容器图标组件
- [ ] `SubWarehouseRow` - 横向子容器滚动区
- [ ] 添加"+"按钮
- [ ] 添加数量徽章

### Phase 4: 右侧物品列表
- [ ] `ItemListRow` - 物品列表项组件
- [ ] `ItemListSection` - 物品列表区域
- [ ] 添加空状态提示
- [ ] 添加加载状态

### Phase 5: 整合与优化
- [ ] 整合到主 `DashboardScreen`
- [ ] 实现容器切换逻辑
- [ ] 实现数据加载逻辑
- [ ] 添加动画效果
- [ ] 性能优化
- [ ] 测试所有交互

### Phase 6: 数据持久化
- [ ] 保存选中容器状态
- [ ] 恢复上次选中的容器
- [ ] 优化加载性能

## 🚀 开始实施

现在开始创建组件...

